<script setup lang="ts">
/*
 * The application's own dashboard, not the panel's.
 *
 * It exists in the examples to prove a point the tests assert on: a panel
 * takes over the URLs it is mounted at and nothing else. This page renders
 * with `panel` null and no navigation, exactly as it did before the panel
 * was installed.
 */
</script>

<template>
    <main>
        <h1>Dashboard</h1>
    </main>
</template>
